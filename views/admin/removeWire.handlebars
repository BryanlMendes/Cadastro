{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<script>
    window.onload = function () {
        const data = new Date().toLocaleString("pt-br");
        document.getElementById('date').innerHTML = data;
        document.getElementById('data').value = data;
        console.log(data)
    }
</script>

<h3>Remover Fios:</h3>

<div class="card">
    <div class="card-body">
        <form action="/motor/nova" method="POST" class="forms">
            <div class="row d-flex justify-content-around">
                <div class="row col-6">    
                    <div class="divInput row">
                        <label class="label" for="os">Nº Espiras:</label>
                        <input type="text" name="os" id="os" placeholder="Nº Espiras" class="input">
                    </div>
                </div>
                <div class="row col-6">
                    <div class="divInput row">
                        <label class="label" for="os">Tipo Ligação:</label>
                        <select class="input" id="voltage" name="voltage">
                            <option selected disabled value="">Tipo Ligação:</option>
                            <option value="220/380V">220/380V</option>
                            <option value="380V">380V</option>
                            <option value="380/660V">380/660V</option>
                            <option value="220/380/440V">220/380/440V</option>
                        </select>
                    </div>
                </div>
               
            <br>
            <div class="d-flex flex-row-reverse"><button type="submit" class="button"><i class="fa-solid fa-floppy-disk"></i> Salvar</button></div>   
        </form>
    </div>
</div>